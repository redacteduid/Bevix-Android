<?xml version="1.0" encoding="utf-8"?>
<!-- list_item.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Icons for the menu item -->
    <ImageView
        android:id="@+id/icon_image_view1"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp" />

    <ImageView
        android:id="@+id/icon_image_view2"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_toEndOf="@id/icon_image_view1"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp" />

    <ImageView
        android:id="@+id/icon_image_view3"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_toEndOf="@id/icon_image_view2"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp" />

    <ImageView
        android:id="@+id/icon_image_view4"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_toEndOf="@id/icon_image_view3"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp" />

    <ImageView
        android:id="@+id/icon_image_view5"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_toEndOf="@id/icon_image_view4"
        android:layout_centerVertical="true" />

    <!-- Container for text views -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_toEndOf="@id/icon_image_view5">

        <!-- Name of the menu item -->
        <TextView
            android:id="@+id/name_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Drink Name"
            android:textSize="24sp"
            android:textStyle="bold" />

        <!-- Description of the menu item -->
        <TextView
            android:id="@+id/description_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Description"
            android:textSize="16sp"
            tools:ignore="HardcodedText" />
    </LinearLayout>
</RelativeLayout>